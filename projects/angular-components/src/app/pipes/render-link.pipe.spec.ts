import { RenderLinkPipe } from './render-link.pipe';

describe('RenderLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new RenderLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
